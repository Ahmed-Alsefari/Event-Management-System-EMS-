
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Service Packages Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
<!-- Top Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
        <span class="navbar-brand">🔧 Admin Panel</span>

        <div class="navbar-nav mx-auto">
            <a class="nav-link" th:href="@{/admin/dashboard}">Dashboard</a>
            <a class="nav-link active" th:href="@{/admin/packages}">Service Packages</a>
            <a class="nav-link" th:href="@{/admin/reservations}">Reservations</a>
        </div>

        <div>
            <form th:action="@{/logout}" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-light">Logout</button>
            </form>
        </div>
    </div>
</nav>

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📦 Service Packages Management</h2>
        <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addPackageModal">
            <i class="fas fa-plus-circle"></i> Add New Package
        </button>
    </div>

    <!-- Success Messages -->
    <div th:if="${param.added}" class="alert alert-success alert-dismissible fade show">
        <i class="fas fa-check-circle"></i> Package added successfully!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div th:if="${param.updated}" class="alert alert-info alert-dismissible fade show">
        <i class="fas fa-edit"></i> Package updated successfully!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div th:if="${param.deleted}" class="alert alert-warning alert-dismissible fade show">
        <i class="fas fa-trash"></i> Package deleted successfully!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div th:if="${param.toggled}" class="alert alert-info alert-dismissible fade show">
        <i class="fas fa-toggle-on"></i> Package status changed!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <!-- Packages Grid -->
    <div class="row g-4">
        <div th:if="${#lists.isEmpty(packages)}" class="col-12">
            <div class="alert alert-info text-center">
                <h4>No service packages available</h4>
                <p>Click "Add New Package" to create your first service package</p>
            </div>
        </div>

        <div class="col-md-6 col-lg-4" th:each="pkg : ${packages}">
            <div class="card shadow h-100">
                <div class="card-header"
                     th:classappend="${pkg.active ? 'bg-success' : 'bg-secondary'}"
                     style="color: white;">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-0" th:text="${pkg.name}"></h5>
                        <span th:if="${pkg.active}" class="badge bg-light text-success">
                            <i class="fas fa-check-circle"></i> Active
                        </span>
                        <span th:if="${!pkg.active}" class="badge bg-light text-dark">
                            <i class="fas fa-times-circle"></i> Inactive
                        </span>
                    </div>
                </div>
                <div class="card-body">
                    <div class="text-center mb-3">
                        <h2 class="text-primary">$<span th:text="${pkg.price}">0</span></h2>
                    </div>
                    <p class="text-muted" th:text="${pkg.description}"></p>

                    <div class="d-grid gap-2">
                        <!-- Edit Button -->
                        <button class="btn btn-warning" data-bs-toggle="modal"
                                th:attr="data-bs-target='#editModal' + ${pkg.id}">
                            <i class="fas fa-edit"></i> Edit Package
                        </button>

                        <!-- Toggle Active/Inactive -->
                        <a th:href="@{'/admin/service/toggle/' + ${pkg.id}}"
                           class="btn"
                           th:classappend="${pkg.active ? 'btn-outline-secondary' : 'btn-outline-success'}">
                            <i th:class="${pkg.active ? 'fas fa-toggle-off' : 'fas fa-toggle-on'}"></i>
                            <span th:text="${pkg.active ? 'Deactivate' : 'Activate'}"></span>
                        </a>

                        <!-- Delete Button -->
                        <a th:href="@{'/admin/service/delete/' + ${pkg.id}}"
                           class="btn btn-danger"
                           onclick="return confirm('Are you sure you want to delete this package? This action cannot be undone.')">
                            <i class="fas fa-trash"></i> Delete
                        </a>
                    </div>
                </div>
                <div class="card-footer text-muted">
                    <small>ID: <span th:text="${pkg.id}"></span></small>
                </div>
            </div>

            <!-- Edit Modal for Each Package -->
            <div class="modal fade" th:id="'editModal' + ${pkg.id}" tabindex="-1">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header bg-warning">
                            <h5 class="modal-title">Edit Service Package</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <form th:action="@{/admin/service/update}" method="post">
                            <div class="modal-body">
                                <input type="hidden" name="id" th:value="${pkg.id}">

                                <div class="mb-3">
                                    <label class="form-label fw-bold">Package Name *</label>
                                    <input type="text" name="name" th:value="${pkg.name}"
                                           class="form-control" required>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label fw-bold">Description *</label>
                                    <textarea name="description" class="form-control" rows="3"
                                              required th:text="${pkg.description}"></textarea>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label fw-bold">Price ($) *</label>
                                    <input type="number" step="0.01" name="price" th:value="${pkg.price}"
                                           class="form-control" required min="0">
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-warning">
                                    <i class="fas fa-save"></i> Update Package
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Add Package Modal -->
<div class="modal fade" id="addPackageModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title">Add New Service Package</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form th:action="@{/admin/service/add}" method="post">
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Package Name *</label>
                        <input type="text" name="name" class="form-control"
                               placeholder="e.g., Premium Wedding Package" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Description *</label>
                        <textarea name="description" class="form-control" rows="3"
                                  placeholder="Describe what's included in this package..." required></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Price ($) *</label>
                        <input type="number" step="0.01" name="price" class="form-control"
                               placeholder="0.00" required min="0">
                    </div>

                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i> New packages are automatically set to <strong>Active</strong> and will be visible to customers immediately.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-plus-circle"></i> Add Package
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

