<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Event Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" th:href="@{/}">🎉 EMS</a>
        <div>
            <a class="btn btn-outline-light me-2" th:href="@{/login}">Login</a>
            <a class="btn btn-light" th:href="@{/register}">Register</a>
        </div>
    </div>
</nav>

<div class="container text-center mt-5">
    <div th:if="${param.logout}" class="alert alert-success col-md-6 mx-auto">
        You have been successfully logged out.
    </div>

    <h1 class="display-4">Welcome to the Event Management System</h1>
    <p class="lead mt-3">Create and manage your events easily online!</p>
    <div class="mt-4">
        <a class="btn btn-primary btn-lg me-2" th:href="@{/register}">Get Started</a>
        <a class="btn btn-outline-primary btn-lg" th:href="@{/login}">Sign In</a>
    </div>
</div>
</body>
</html>
