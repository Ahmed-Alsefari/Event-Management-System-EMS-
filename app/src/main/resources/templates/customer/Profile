<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Profile - EMS Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .event-card {
            transition: all 0.3s;
            border-left: 4px solid #667eea;
        }
        .event-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .status-badge {
            font-size: 0.85rem;
            padding: 0.4rem 0.8rem;
        }
    </style>
</head>
<body>
<!-- Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
        <span class="navbar-brand">🎉 Event Management System</span>

        <div class="navbar-nav mx-auto">
            <a class="nav-link" th:href="@{/dashboard}">Home</a>
            <a class="nav-link" th:href="@{/dashboard/services}">Services</a>
            <a class="nav-link" th:href="@{/dashboard/book}">Book</a>
            <a class="nav-link active" th:href="@{/dashboard/profile}">Profile</a>
        </div>

        <div class="d-flex align-items-center">
            <form th:action="@{/logout}" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-light btn-sm">Logout</button>
            </form>
        </div>
    </div>
</nav>

<!-- Profile Header -->
<div class="bg-info text-white py-4">
    <div class="container text-center">
        <h1 class="display-5">👤 My Profile</h1>
        <p class="lead">Manage your account and view your event bookings</p>
    </div>
</div>

<div class="container mt-5 mb-5">
    <!-- Success Messages -->
    <div th:if="${param.eventCreated}" class="alert alert-success alert-dismissible fade show">
        <i class="fas fa-check-circle"></i> Event booking submitted successfully! Waiting for admin confirmation.
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div th:if="${param.eventUpdated}" class="alert alert-info alert-dismissible fade show">
        <i class="fas fa-info-circle"></i> Event updated successfully!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div th:if="${param.eventCancelled}" class="alert alert-warning alert-dismissible fade show">
        <i class="fas fa-exclamation-circle"></i> Event cancelled successfully.
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div class="row">
        <!-- User Information Card -->
        <div class="col-md-4 mb-4">
            <div class="card shadow">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="fas fa-user"></i> Personal Information</h5>
                </div>
                <div class="card-body">
                    <div th:if="${param.updated}" class="alert alert-success alert-dismissible fade show">
                        Profile updated successfully!
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>

                    <form th:action="@{/dashboard/profile/update}" method="post">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Full Name</label>
                            <input type="text" name="name" th:value="${user.name}"
                                   class="form-control" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Email</label>
                            <input type="email" th:value="${user.email}"
                                   class="form-control" disabled>
                            <div class="form-text">Email cannot be changed</div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Phone Number</label>
                            <input type="text" name="phone" th:value="${user.phone}"
                                   class="form-control" placeholder="Optional">
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Account Type</label>
                            <input type="text" th:value="${user.role}"
                                   class="form-control" disabled>
                        </div>

                        <button type="submit" class="btn btn-info w-100">
                            <i class="fas fa-save"></i> Update Profile
                        </button>
                    </form>
                </div>
            </div>

            <!-- Statistics Card -->
            <div class="card shadow mt-4">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0"><i class="fas fa-chart-bar"></i> Quick Stats</h5>
                </div>
                <div class="card-body">
                    <div class="d-flex justify-content-between mb-3">
                        <span><i class="fas fa-calendar"></i> Total Events:</span>
                        <strong th:text="${#lists.size(events)}"></strong>
                    </div>
                    <div class="d-flex justify-content-between mb-3">
                        <span><i class="fas fa-check text-success"></i> Confirmed:</span>
                        <strong class="text-success" th:text="${events.?[status == 'Confirmed'].size()}"></strong>
                    </div>
                    <div class="d-flex justify-content-between">
                        <span><i class="fas fa-clock text-warning"></i> Pending:</span>
                        <strong class="text-warning" th:text="${events.?[status == 'Pending'].size()}"></strong>
                    </div>
                </div>
            </div>
        </div>

        <!-- My Event Bookings -->
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-calendar-check"></i> My Event Bookings</h5>
                </div>
                <div class="card-body">
                    <div th:if="${#lists.isEmpty(events)}" class="text-center py-5">
                        <i class="fas fa-calendar-times fa-5x text-muted mb-3"></i>
                        <h4 class="mt-3">No event bookings yet</h4>
                        <p class="text-muted">Start by booking your first event!</p>
                        <a th:href="@{/dashboard/book}" class="btn btn-primary mt-3">
                            <i class="fas fa-plus-circle"></i> Book Event Now
                        </a>
                    </div>

                    <!-- Event Cards View -->
                    <div th:unless="${#lists.isEmpty(events)}">
                        <div class="row g-3">
                            <div class="col-md-12" th:each="e : ${events}">
                                <div class="card event-card">
                                    <div class="card-body">
                                        <div class="row">
                                            <div class="col-md-8">
                                                <h5 class="card-title">
                                                    <i class="fas fa-calendar-alt text-primary"></i>
                                                    <span th:text="${e.title}"></span>
                                                </h5>
                                                <p class="card-text text-muted mb-2" th:text="${e.description}"></p>

                                                <div class="row mt-3">
                                                    <div class="col-6">
                                                        <small class="text-muted">
                                                            <i class="fas fa-map-marker-alt"></i>
                                                            <span th:text="${e.location}"></span>
                                                        </small>
                                                    </div>
                                                    <div class="col-6">
                                                        <small class="text-muted">
                                                            <i class="fas fa-calendar"></i>
                                                            <span th:text="${e.date}"></span>
                                                        </small>
                                                    </div>
                                                    <div class="col-6 mt-2">
                                                        <small class="text-muted">
                                                            <i class="fas fa-users"></i>
                                                            <span th:text="${e.seats + ' guests'}"></span>
                                                        </small>
                                                    </div>
                                                    <div class="col-6 mt-2">
                                                        <small class="text-muted">
                                                            <i class="fas fa-box"></i>
                                                            <span th:if="${e.servicePackage != null}"
                                                                  th:text="${e.servicePackage.name}"></span>
                                                            <span th:if="${e.servicePackage == null}">No package</span>
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="col-md-4 text-end">
                                                <span th:class="${'status-badge badge ' +
                                                              (e.status == 'Confirmed' ? 'bg-success' :
                                                               (e.status == 'Cancelled' ? 'bg-danger' : 'bg-warning'))}"
                                                      th:text="${e.status}"></span>

                                                <div class="mt-3">
                                                    <a th:if="${e.status == 'Pending'}"
                                                       th:href="@{'/dashboard/events/edit/' + ${e.id}}"
                                                       class="btn btn-sm btn-outline-primary mb-2 w-100">
                                                        <i class="fas fa-edit"></i> Modify
                                                    </a>

                                                    <a th:if="${e.status != 'Cancelled'}"
                                                       th:href="@{'/dashboard/events/cancel/' + ${e.id}}"
                                                       class="btn btn-sm btn-outline-danger w-100"
                                                       onclick="return confirm('Are you sure you want to cancel this event?')">
                                                        <i class="fas fa-times-circle"></i> Cancel
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
