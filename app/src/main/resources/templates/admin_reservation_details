<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Reservation Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
<!-- Top Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
        <span class="navbar-brand">🔧 Admin Panel</span>

        <div class="navbar-nav mx-auto">
            <a class="nav-link" th:href="@{/admin/dashboard}">Dashboard</a>
            <a class="nav-link" th:href="@{/admin/packages}">Service Packages</a>
            <a class="nav-link active" th:href="@{/admin/reservations}">Reservations</a>
        </div>

        <div>
            <form th:action="@{/logout}" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-light">Logout</button>
            </form>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <div class="mb-4">
        <a th:href="@{/admin/reservations}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to All Reservations
        </a>
    </div>

    <!-- Success Message -->
    <div th:if="${param.updated}" class="alert alert-success alert-dismissible fade show">
        <i class="fas fa-check-circle"></i> Event status updated and notification sent!
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>

    <div class="card shadow">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0"><i class="fas fa-info-circle"></i> Reservation Details</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered">
                    <tbody>
                    <tr>
                        <th width="20%" class="bg-light">ID</th>
                        <td th:text="${event.id}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Title</th>
                        <td th:text="${event.title}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Description</th>
                        <td th:text="${event.description}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Date</th>
                        <td th:text="${event.date}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Location</th>
                        <td th:text="${event.location}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Seats</th>
                        <td th:text="${event.seats}"></td>
                    </tr>
                    <tr>
                        <th class="bg-light">Package</th>
                        <td>
                                <span th:if="${event.servicePackage != null}">
                                    <strong th:text="${event.servicePackage.name}"></strong>
                                    <br>
                                    <small class="text-muted" th:text="${event.servicePackage.description}"></small>
                                    <br>
                                    <span class="badge bg-success">$<span th:text="${event.servicePackage.price}"></span></span>
                                </span>
                            <span th:if="${event.servicePackage == null}" class="text-muted">No package selected</span>
                        </td>
                    </tr>
                    <tr>
                        <th class="bg-light">Current Status</th>
                        <td>
                                <span th:class="${'badge fs-6 ' +
                                    (event.status == 'Confirmed' ? 'bg-success' :
                                     event.status == 'Cancelled' ? 'bg-danger' : 'bg-warning')}"
                                      th:text="${event.status}"></span>
                        </td>
                    </tr>
                    </tbody>
                </table>
            </div>

            <hr class="my-4">

            <!-- Actions Section -->
            <h5 class="mb-3"><i class="fas fa-cogs"></i> Actions</h5>

            <form th:action="@{/admin/events/update-status}" method="post" class="mb-3">
                <input type="hidden" name="id" th:value="${event.id}">

                <div class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label class="form-label fw-bold">Update Status</label>
                        <select name="status" class="form-select">
                            <option value="Pending" th:selected="${event.status == 'Pending'}">Pending</option>
                            <option value="Confirmed" th:selected="${event.status == 'Confirmed'}">Confirmed</option>
                            <option value="Cancelled" th:selected="${event.status == 'Cancelled'}">Cancelled</option>
                        </select>
                    </div>

                    <div class="col-md-4">
                        <button type="submit" class="btn btn-success w-100">
                            <i class="fas fa-paper-plane"></i> Update & Notify Customer
                        </button>
                    </div>

                    <div class="col-md-4">
                        <a th:href="@{'/admin/events/delete/' + ${event.id}}"
                           class="btn btn-danger w-100"
                           onclick="return confirm('Are you sure you want to delete this reservation? This action cannot be undone.')">
                            <i class="fas fa-trash"></i> Delete Reservation
                        </a>
                    </div>
                </div>
            </form>

            <div class="alert alert-info mt-4">
                <i class="fas fa-info-circle"></i>
                <strong>Note:</strong> When you update the status, an email notification will be automatically sent to the customer.
            </div>
        </div>
    </div>

    <!-- Back Button at Bottom -->
    <div class="mt-4 mb-5 text-center">
        <a th:href="@{/admin/reservations}" class="btn btn-lg btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to All Reservations
        </a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
